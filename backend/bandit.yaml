# Bandit config (YAML). Use: bandit -r app -c bandit.yaml -ll
exclude_dirs:
  - venv
  - .venv
  - vector_db
  - experiments
  - data_pipeline

skips:
  - B101 # assert used in dev / smoke paths
  - B301 # trusted local chunks.pkl from our RAG pipeline
