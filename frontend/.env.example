# When front and back are on the public internet, set this to your API origin (no /api path).
# Example: VITE_API_ORIGIN=https://smartlearner-api.up.railway.app
# Leave unset to use http://localhost:8000 for local dev.
